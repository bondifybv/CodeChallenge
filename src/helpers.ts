type numberOrString = number | string;

export const isPalindrome = (str: string): boolean => {
  return false;
};

export const removeDuplicates = (arr: numberOrString[]): numberOrString[] => {
  return [];
};

export const contains = (object: any, value: numberOrString): boolean => {
  return false;
};

export const sumToTarget = (arr: number[], target: number) => {
  return [];
};
